package me.erken.efe.chess.model;

public class CastlingMove extends Move {

    public CastlingMove(Player player, Square source, Square destination) {
        super(player, source, destination);
    }
}
